import React from 'react';
import { StyleSheet } from 'react-native';
import { View, Text, TouchableOpacity } from 'react-native';

export default function Answer(props) {
    return (
        <View>
            <Text>This is the an answer</Text>
        </View>
    )
}