import React from 'react'
import { Text } from 'react-native'
import { View } from 'react-native'

export default function AddDeck() {
    return (
        <View>
            <Text>This is the add deck</Text>
        </View>
    )
}